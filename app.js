const counter = document.getElementById('num');

let item = document.getElementsByClassName('btn')

let score = 0

btn[0].addEventListener('click',()=>{
        score--
        zero.textContect=score
});

btn[1].addEventListener('click',()=>{
    score = 0
    zero.textContect=score
});

btn[2].addEventListener('click',()=>{
    score++
    zero.textContect=score
});